import React from 'react';
import React
// Create a new component. This component produce some HTML

const App = function() {
  return <div>Hi!</div>
}

//Take this component's generate some HTML and put it on the page
React.render(App);
